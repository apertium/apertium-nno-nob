Norwegian Nynorsk and Norwegian Bokmål

                            apertium-nn-nb
===============================================================================

This language pair is for translating between Norwegian Nynorsk and Norwegian 
Bokmål.

Requirements
===============================================================================

* lttoolbox (>= 3.1.2)
* apertium (>= 3.1.1)
* vislcg3 (>= 0.9.7.5129)

Compiling
===============================================================================

Given the requirements being installed, you should be able to just run:

$ ./configure 
$ make
# make install

You can use ./autogen.sh instead of ./configure you're compiling from
SVN. If you're using a --prefix to ./configure, make sure it's the
same one you used to install apertium itself.

Testing
===============================================================================

If you are in the source directory after running make, the following
commands should work:

$  echo "Eg vil gå ein tur" | apertium -d . nn-nb
Jeg vil gå en tur

$ echo "Jeg vil gå en tur" | apertium -d . nb-nn
Eg vil gå ein tur

After installing somewhere in $PATH, you should be able to do eg.

$ echo "Jeg vil gå en tur" | apertium nb-nn
Eg vil gå ein tur

The following command runs tests which are on the Apertium wiki page:

$ ./regression-tests.sh 

See also
===============================================================================

* http://wiki.apertium.org/wiki/Installation
* http://wiki.apertium.org/wiki/Norsk
